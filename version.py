"""Project-level version for tradinebotte (x.y.z).

Only a GitHub Release bumps the minor (x.y); merges to main without a GitHub
Release bump the patch (z) — e.g. 0.87 -> 0.87.1. Keep this in sync with the
latest CHANGELOG entry at release time.
"""
__version__ = "0.90"
